<div dir="rtl">

# دليل المستخدم - نظام HWA Program

## 📋 المحتويات

1. [مقدمة](#مقدمة)
2. [تسجيل الدخول](#تسجيل-الدخول)
3. [دليل المستخدم العادي (User)](#دليل-المستخدم-العادي)
   - [الصفحة الرئيسية](#الصفحة-الرئيسية)
   - [إنشاء عمليات جديدة](#إنشاء-عمليات-جديدة)
   - [السلة والدفع](#السلة-والدفع)
   - [طباعة الفاتورة](#طباعة-الفاتورة)
4. [دليل المدير (Admin)](#دليل-المدير-admin)
   - [لوحة التحكم](#لوحة-التحكم)
   - [إدارة الطلبات](#إدارة-الطلبات)
   - [إعادة تعيين أرقام الطلبات](#إعادة-تعيين-أرقام-الطلبات)
5. [الأسئلة الشائعة](#الأسئلة-الشائعة)
6. [حل المشاكل](#حل-المشاكل)

---

## مقدمة

**نظام HWA Program** هو نظام إلكتروني متكامل لإدارة عمليات الإنتاج الصناعي. يتيح لك النظام:

- إنشاء وإدارة طلبات العمليات الصناعية المختلفة
- حساب تلقائي للأسعار والأوزان
- طباعة الفواتير التفصيلية
- متابعة حالة الطلبات

### أنواع المستخدمين

يوجد نوعان من المستخدمين في النظام:

1. **المستخدم العادي (User)**: يمكنه إنشاء الطلبات وإدارة عملياته الخاصة
2. **المدير (Admin)**: يمكنه مشاهدة جميع الطلبات والإحصائيات وإدارة النظام

---

## تسجيل الدخول

### للمستخدم العادي
1. اذهب إلى صفحة تسجيل الدخول: `/user/login`
2. أدخل اسم المستخدم وكلمة المرور
3. اضغط على زر "تسجيل الدخول"
4. سيتم توجيهك تلقائيًا إلى الصفحة الرئيسية

### للمدير (Admin)
1. اذهب إلى صفحة تسجيل الدخول للإدارة: `/Admin/login`
2. أدخل بيانات الدخول الإدارية
3. سيتم توجيهك إلى لوحة التحكم

> **ملاحظة مهمة**: صلاحيات الأدمن منفصلة تمامًا عن المستخدم العادي. كل نوع له نظام تسجيل دخول مستقل.

---

## دليل المستخدم العادي

### الصفحة الرئيسية

عند تسجيل الدخول، سترى:
- رسالة ترحيب تحتوي على اسمك: **"مرحبًا بك [اسمك]"**
- قائمة جانبية تحتوي على جميع العمليات المتاحة
- نظرة عامة على حسابك

### إنشاء عمليات جديدة

النظام يوفر العديد من العمليات الصناعية:

#### 1. اللحام
- انتقل إلى "اللحام" من القائمة الجانبية
- أدخل البيانات المطلوبة (الأبعاد، الكمية، إلخ)
- اضغط على "تأكيد" لإضافة العملية إلى الطلب

#### 2. الدرفلة
- انتقل إلى "الدرفلة" من القائمة الجانبية
- أدخل البيانات المطلوبة
- اضغط على "تأكيد"

#### 3. التقطيع
يوفر النظام ثلاثة أنواع من التقطيع:
- **تقطيع ألواح**: لتقطيع الألواح المعدنية
- **تقطيع لمبة**: لتقطيع قطاعات اللمبة
- **تقطيع بلتات**: لتقطيع البلتات

**كيفية الاستخدام:**
1. انتقل إلى "التقطيع" من القائمة الجانبية
2. اختر نوع التقطيع المطلوب
3. أدخل البيانات (الطول، العرض، الكمية، إلخ)
4. اضغط على "تأكيد"

#### 4. التناية (الأكثر استخدامًا)
يوفر النظام ثلاثة أنواع من التناية:
- **تناية ألواح**: لثني الألواح المعدنية
- **تناية حليات**: لثني الحليات
- **تناية بلتات**: لثني البلتات

**خطوات تناية الألواح (مثال):**

1. انتقل إلى "التناية" → "تناية ألواح"
2. أدخل البيانات التالية:
   - **الطول**: بالسنتيمتر (مثال: 100)
   - **العرض**: بالسنتيمتر (مثال: 50)
   - **السمك**: بالملليمتر (مثال: 1.5 مم)
   - **الكمية**: عدد القطع
   - **عدد التنايات**: عدد مرات الثني في القطعة الواحدة

3. اضغط على "تأكيد"

> **ملاحظة مهمة عن السمك**: 
> - يمكنك إدخال أي قيمة من **0.1 مم** إلى **12 مم**
> - إذا كان السمك **أقل من 1 مم** (مثل 0.2، 0.5، 0.8)، سيتم استخدام سعر السمك 1 مم تلقائيًا
> - إذا كان السمك **من 1 إلى 9 مم**، سيتم استخدام سعر السمك المقابل (يُقرب للرقم الصحيح)
> - إذا كان السمك **10، 11، أو 12 مم**، سيتم استخدام سعر واحد لهذه الفئة

**مثال على الحساب:**
- طول: 100 سم
- عرض: 50 سم
- سمك: 0.5 مم
- كمية: 10 قطع
- عدد التنايات: 2
- **النتيجة**: سيتم حساب السعر على أساس سمك 1 مم × 10 قطع × 2 تناية

#### 5. التخريم
- انتقل إلى "التخريم" من القائمة الجانبية
- أدخل البيانات المطلوبة
- اضغط على "تأكيد"

#### 6. الخامات
يوفر النظام نوعين من الخامات:
- **خامات عادية**: للمواد القياسية
- **خامات استاندرد**: للمواد ذات المواصفات الخاصة

**كيفية الطلب:**
1. انتقل إلى "الخامات" من القائمة الجانبية
2. اختر نوع الخامة (عادية أو استاندرد)
3. أدخل البيانات المطلوبة
4. اضغط على "تأكيد"

---

### السلة والدفع

#### السلة (Cart)
- انتقل إلى "السلة" من القائمة الجانبية أو أيقونة السلة في الأعلى
- ستجد جميع العمليات التي أضفتها للطلب الحالي
- يمكنك:
  - **مراجعة** جميع العمليات المضافة
  - **حذف** أي عملية غير مرغوب فيها
  - **رؤية الإجمالي** للطلب

#### إتمام الطلب (Checkout)
1. في صفحة السلة، راجع جميع العمليات
2. تأكد من صحة البيانات
3. اضغط على زر **"إتمام الطلب"** أو **"الدفع"**
4. سيتم تحويل حالة الطلب من **"غير مدفوع"** إلى **"مدفوع"**
5. سيظهر رقم الطلب والفاتورة

> **مهم**: 
> - لا يمكنك تعديل الطلب بعد الدفع
> - سيتم إنشاء رقم طلب ورقم فاتورة فريد لكل طلب

---

### طباعة الفاتورة

بعد إتمام الطلب:
1. اضغط على زر **"طباعة الفاتورة"** أو **"Print"**
2. ستفتح نافذة جديدة تحتوي على الفاتورة التفصيلية
3. الفاتورة تحتوي على:
   - رقم الطلب (Order Number)
   - رقم الفاتورة (Invoice Number)
   - تاريخ الطلب
   - اسم المستخدم
   - جدول مفصل بجميع العمليات
   - الأسعار والأوزان
   - الإجمالي النهائي

4. يمكنك:
   - **الطباعة مباشرة** من المتصفح (Ctrl+P)
   - **الحفظ كـ PDF**
   - **مشاركة الفاتورة**

---

## دليل المدير (Admin)

### لوحة التحكم

بعد تسجيل الدخول كـ Admin، ستصل إلى لوحة التحكم التي تحتوي على:

#### 1. رسالة الترحيب
- **"مرحبًا بك [اسم الأدمن]"** في الجزء العلوي من الصفحة

#### 2. إحصائيات الطلبات
- **إجمالي الطلبات**: عدد جميع الطلبات **المدفوعة فقط**
- **طلبات اليوم**: عدد الطلبات المدفوعة اليوم
- **الإيرادات**: إجمالي الإيرادات من الطلبات المدفوعة

> **ملاحظة مهمة**: 
> - جميع الإحصائيات تحتسب **الطلبات المدفوعة فقط** (حالة `paied`)
> - الطلبات غير المدفوعة (حالة `unpaid`) لا تظهر في الإحصائيات
> - هذا يضمن دقة البيانات المالية

#### 3. الرسوم البيانية
- رسم بياني يوضح عدد الطلبات المدفوعة حسب اليوم
- رسم بياني للإيرادات
- إحصائيات أخرى حسب احتياجات الإدارة

---

### إدارة الطلبات

من لوحة التحكم، يمكنك:
- **عرض جميع الطلبات**: قائمة شاملة بجميع الطلبات المدفوعة
- **البحث والتصفية**: البحث عن طلب معين حسب الرقم أو التاريخ أو العميل
- **عرض التفاصيل**: مشاهدة تفاصيل أي طلب
- **طباعة الفواتير**: طباعة فاتورة أي طلب

---

### إعادة تعيين أرقام الطلبات

> ⚠️ **تحذير: هذه الميزة حساسة جدًا ومخصصة فقط لبيئة التطوير!**

#### من يمكنه استخدامها؟
هذه الميزة متاحة **فقط** للأدمن الذي اسمه **`cost.accounting`** أو **`cost accounting`**

#### كيفية الاستخدام:
1. سجل الدخول كـ `cost.accounting`
2. في لوحة التحكم، ستجد إشعارًا برتقالي في الأعلى
3. الإشعار يحتوي على:
   - **عنوان**: "إعادة تعيين أرقام الطلبات"
   - **وصف**: "سيتم حذف جميع الطلبات وإعادة تعيين الأرقام ليبدأ من 1"
   - **زر**: "إعادة تعيين الأرقام"

4. عند الضغط على الزر، سيظهر **تحذير SweetAlert2**:
   - **العنوان**: "هل أنت متأكد؟"
   - **النص**: "سيتم حذف جميع الطلبات الحالية! هذا الإجراء لا يمكن التراجع عنه!"
   - **خيارين**: "نعم، تأكيد" أو "إلغاء"

5. إذا أكدت:
   - سيتم حذف **جميع الطلبات** (orders) و**تفاصيلها** (orderdetailes)
   - سيتم إعادة تعيين `AUTO_INCREMENT` ليبدأ من 1
   - سيظهر رسالة نجاح: "تم إعادة تعيين أرقام الطلبات بنجاح!"
   - سيتم إعادة تحميل الصفحة تلقائيًا

#### متى تستخدم هذه الميزة؟
- **في بيئة التطوير فقط**: عند اختبار النظام ووجود بيانات تجريبية
- **قبل التشغيل الفعلي**: لإعادة ضبط النظام قبل البدء بالعمل الفعلي
- **NEVER في بيئة الإنتاج**: أبدًا لا تستخدمها في نظام به طلبات حقيقية!

#### ماذا يحدث بالضبط؟
```sql
-- يتم تعطيل فحص المفاتيح الأجنبية مؤقتًا
SET FOREIGN_KEY_CHECKS=0;

-- حذف جميع تفاصيل الطلبات
DELETE FROM orderdetailes;

-- حذف جميع الطلبات
DELETE FROM orders;

-- إعادة تفعيل فحص المفاتيح الأجنبية
SET FOREIGN_KEY_CHECKS=1;

-- إعادة تعيين العداد ليبدأ من 1
ALTER TABLE orders AUTO_INCREMENT = 1;
```

> ⚠️ **تحذير نهائي**: هذا الإجراء يحذف البيانات بشكل دائم ولا يمكن التراجع عنه!

---

## الأسئلة الشائعة

### 1. لماذا لا يظهر الطلب في لوحة التحكم (Admin)؟
**السبب**: لوحة التحكم تعرض فقط الطلبات **المدفوعة** (حالة `paied`).

**الحل**: تأكد من أنك أتممت عملية الدفع (Checkout) للطلب.

---

### 2. هل يمكنني تعديل الطلب بعد الدفع؟
**الجواب**: لا، بمجرد تغيير حالة الطلب إلى "مدفوع"، لا يمكن تعديله. هذا لضمان سلامة البيانات المالية.

---

### 3. ماذا يحدث إذا أدخلت سمك أقل من 1 مم في تناية الألواح؟
**الجواب**: النظام سيقبل القيمة ويستخدم سعر السمك 1 مم تلقائيًا. على سبيل المثال:
- سمك 0.2 مم → يستخدم سعر 1 مم
- سمك 0.5 مم → يستخدم سعر 1 مم
- سمك 0.8 مم → يستخدم سعر 1 مم

---

### 4. ما الفرق بين User و Admin؟
**الفروقات الرئيسية**:

| الميزة | User | Admin |
|-------|------|-------|
| إنشاء طلبات | ✅ نعم | ❌ لا |
| مشاهدة طلباته فقط | ✅ نعم | ❌ لا |
| مشاهدة جميع الطلبات | ❌ لا | ✅ نعم |
| الإحصائيات | ❌ لا | ✅ نعم |
| إعادة تعيين الأرقام | ❌ لا | ✅ نعم (cost.accounting فقط) |
| نظام الدخول | `/user/login` | `/Admin/login` |
| Guard | `web` | `Admin` |

---

### 5. لماذا لا يظهر زر "إعادة تعيين أرقام الطلبات"؟
**الأسباب المحتملة**:
1. لست مسجل دخول كـ Admin
2. اسم المستخدم الخاص بك ليس `cost.accounting` أو `cost accounting`
3. أنت في بيئة إنتاج (production) والزر معطل للأمان

---

### 6. كيف أحسب الوزن الكلي للطلب؟
**الجواب**: النظام يحسب الوزن تلقائيًا بناءً على:
- الطول × العرض × السمك × الكثافة النوعية للمادة
- يظهر الوزن في تفاصيل الطلب والفاتورة

---

## حل المشاكل

### المشكلة: SweetAlert لا يظهر أو يظهر بدون نص

**الأعراض**:
- تظهر نافذة SweetAlert فارغة
- النص غير واضح أو لا يظهر
- الأزرار موجودة لكن النص مفقود

**الحل**:
1. تأكد من تحميل ملفات SweetAlert2 بالترتيب الصحيح:
```html
<!-- في head -->
<link href="assets/plugins/sweet-alert2/sweetalert2.min.css" rel="stylesheet">
<link href="assets/plugins/animate/animate.min.css" rel="stylesheet">

<!-- في footer، قبل @yield('scripts') -->
<script src="assets/plugins/sweet-alert2/sweetalert2.min.js"></script>
<script src="assets/pages/sweet-alert.init.js"></script>
@yield('scripts')
```

2. تأكد من عدم وجود تحميل مزدوج لـ SweetAlert من CDN

3. امسح الـ cache:
```bash
php artisan cache:clear
php artisan view:clear
```

---

### المشكلة: لا يمكنني إدخال سمك أقل من 1 مم

**الحل**: 
- تأكد من أن حقل السمك يحتوي على `min="0.1"` في HTML
- إذا كانت المشكلة قائمة، تحقق من كود الـ Controller
- تم حل هذه المشكلة في آخر تحديث

---

### المشكلة: خطأ في قاعدة البيانات عند إعادة تعيين الأرقام

**الخطأ**:
```
SQLSTATE[42000]: Cannot truncate a table referenced in a foreign key constraint
```

**الحل**: 
تم حل هذه المشكلة بتعطيل فحص المفاتيح الأجنبية مؤقتًا. إذا ظهر الخطأ:
1. تأكد من أنك تستخدم آخر نسخة من الكود
2. تحقق من الصلاحيات في قاعدة البيانات

---

### المشكلة: الإحصائيات في لوحة التحكم غير صحيحة

**السبب المحتمل**: الإحصائيات تحتسب الطلبات المدفوعة فقط.

**الحل**:
1. تأكد من أن الطلبات التي تبحث عنها حالتها `paied` وليس `unpaid`
2. تحقق من تاريخ الدفع (`paid_at`)
3. أعد تحميل الصفحة

---

### المشكلة: لا يمكنني الدخول للنظام

**للمستخدم**:
1. تأكد من استخدام الرابط الصحيح: `/user/login`
2. تحقق من اسم المستخدم وكلمة المرور
3. تأكد من أن حسابك مفعّل

**للأدمن**:
1. تأكد من استخدام الرابط الصحيح: `/Admin/login`
2. تحقق من بيانات الدخول الإدارية
3. الأدمن له guard مختلف عن المستخدم العادي

---

## نصائح للاستخدام الأمثل

### للمستخدم العادي:
1. ✅ راجع جميع البيانات قبل الضغط على "تأكيد"
2. ✅ تحقق من السلة قبل إتمام الطلب
3. ✅ احتفظ بنسخة من الفاتورة المطبوعة
4. ✅ لاحظ أن الطلبات غير المدفوعة يمكن تعديلها
5. ❌ لا تتأخر في إتمام الطلب (الدفع) لتجنب الالتباس

### للمدير:
1. ✅ راجع الإحصائيات بشكل دوري
2. ✅ تحقق من حالة الطلبات (paied/unpaid)
3. ✅ استخدم التصفية والبحث للوصول السريع للطلبات
4. ⚠️ **لا تستخدم** ميزة إعادة تعيين الأرقام في بيئة الإنتاج
5. ⚠️ خذ نسخة احتياطية قبل أي إجراء حساس

---

## تحديثات النظام

### آخر التحديثات:

**[2025-01-07] تحديث تناية الألواح**
- إضافة دعم للقيم أقل من 1 مم في السمك
- تحسين حساب الأسعار للأسماك الصغيرة

**[السابق] تحديثات الأمان والواجهة**
- إصلاح مشكلة SweetAlert2 في لوحة التحكم
- تحسين عرض اسم المستخدم في الصفحات
- إضافة ميزة إعادة تعيين أرقام الطلبات للتطوير
- إصلاح إحصائيات الطلبات لعرض المدفوعة فقط

---

## للدعم الفني

إذا واجهت أي مشكلة غير مذكورة في هذا الدليل، يرجى:
1. التحقق من سجلات الأخطاء (logs)
2. التواصل مع فريق التطوير
3. توفير تفاصيل المشكلة (رسالة الخطأ، الخطوات المتبعة، لقطة شاشة)

---

**تم إعداد هذا الدليل بتاريخ: يناير 2025**

</div>

